function H = calculateH(angle, l)
    %H = [
    %   ∂r1/∂x   ∂r1/∂y   ∂r1/∂theta
    %   ∂r2/∂x   ∂r2/∂y   ∂r2/∂theta
    %]
    %TODO, make sure r2 and r2 are not mixed up!
    H = [0, 1, -(l/2)/(cos(angle)^2);
    0, 1,  (l/2)/(cos(angle)^2)];
end